{
  "name": "katy-marchant-portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "npx @11ty/eleventy --input=content --serve",
    "css:dev": "sass src/styles/main.scss public/styles.css --style=expanded --watch",
    "css:final": "sass src/styles/main.scss public/styles.css --style=compressed",
    "dev": "npm run serve && npm run css:dev",
    "deploy": "npx @11ty/eleventy --input=content && npm run css:final"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/evecrabb/katy-marchant-portfolio.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/evecrabb/katy-marchant-portfolio/issues"
  },
  "homepage": "https://github.com/evecrabb/katy-marchant-portfolio#readme",
  "devDependencies": {
    "@11ty/eleventy": "^0.10.0"
  },
  "dependencies": {
    "sass": "^1.22.5"
  }
}
